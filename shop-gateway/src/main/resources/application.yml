server:
  port: 7000

spring:
  application:
    name: service-gateway
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848  #nacos中心地址
    gateway:
      discovery:
        locator:
          enabled: true   # 让gateway可以发现nacos中的微服务